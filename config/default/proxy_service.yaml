apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: netmaker-k8s-ops
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager-proxy-service
  namespace: system
spec:
  ports:
  - name: proxy
    port: 8085
    protocol: TCP
    targetPort: proxy
  selector:
    control-plane: controller-manager
