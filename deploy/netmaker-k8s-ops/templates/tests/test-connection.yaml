apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netmaker-k8s-ops.fullname" . }}-test-connection"
  labels:
    {{- include "netmaker-k8s-ops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netmaker-k8s-ops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
